import { handleCapture } from "@/lib/action-center/intake";

export const POST = handleCapture;
export const GET = handleCapture;
export const PUT = handleCapture;
export const PATCH = handleCapture;
export const DELETE = handleCapture;
export const HEAD = handleCapture;
export const OPTIONS = handleCapture;
